pdfplumber==0.11.4
python-docx==1.1.2
pydantic==2.9.2
# OCR fallback for scanned (image-only) PDFs: render with PyMuPDF, recognise
# with Tesseract (the tesseract-ocr binary is installed in the Dockerfile).
PyMuPDF==1.24.10
pytesseract==0.3.13
Pillow==10.4.0
# LLM provider SDKs (used by the respective adapters in app/llm):
anthropic==0.69.0
openai==1.51.2
# Pin httpx < 0.28: openai/anthropic 1.51/0.69 pass the `proxies=` kwarg that
# httpx 0.28 removed (else OpenAI()/Anthropic() construction raises TypeError).
httpx==0.27.2
# HTTP API for the live-workflow dashboard (app/api.py).
fastapi==0.115.0
uvicorn==0.30.6
python-multipart==0.0.9
# Redis — Celery broker, cross-replica SSE pub/sub, config cache (enterprise backbone).
redis==5.0.8
# Celery — durable job execution (worker fleet), replacing daemon threads.
celery==5.4.0
# Auth + persistence (Sprint 1a: users, JWT + refresh, per-user jobs).
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
passlib==1.7.4
bcrypt==4.0.1
python-jose[cryptography]==3.3.0
email-validator==2.2.0
# Stripe subscription billing (PAY-04). Optional at runtime — billing endpoints
# return 503 if STRIPE_SECRET_KEY is unset — but pinned so Docker images include it.
stripe==10.12.0
